================================================================
 RETROAI AGENT - JOURNAL DE BORD
================================================================
 Ce fichier trace, a chaque changement :
   - les ETAPES suivies
   - les BUGS / ERREURS rencontres et leur correction
   - les OBSTACLES et decisions techniques
 Mis a jour par l'assistant a chaque modification du projet.
================================================================


----------------------------------------------------------------
[2026-06-18] ETAPE 1 - Fondations (config + structure)
----------------------------------------------------------------
OBJECTIF :
  Poser le socle du projet : structure du package, dependances,
  et gestion centralisee de la configuration.

FICHIERS CREES :
  - .env.example                      (modele de configuration)
  - requirements.txt                  (requests obligatoire, rich optionnel)
  - retroai_agent/__init__.py         (marque le dossier comme package Python)
  - retroai_agent/config.py           (dataclass Config + load_config())

DECISIONS TECHNIQUES :
  - Parser .env "maison" (zero dependance) pour rester ultra-leger,
    conformement au cahier des charges (cible : Core 2 Duo, 4 Go RAM).
  - Variables d'environnement SYSTEME prioritaires sur le fichier .env
    (on n'ecrase pas une variable deja definie).
  - Validation "fail fast" : SystemExit immediat si NVIDIA_API_KEY absente,
    plutot qu'un 401 obscur plus tard.
  - Helpers _env_bool / _env_int car os.environ ne renvoie que des chaines.

OBSTACLES / BUGS :
  - [ENV] L'utilisateur etait dans cmd.exe (et non PowerShell) :
    "Copy-Item" non reconnu. -> Solution : utiliser "copy .env.example .env".
    A retenir : verifier l'invite (C:\...> = cmd, PS C:\...> = PowerShell).

TESTS REALISES :
  - Chargement avec fausse cle           -> OK (valeurs correctes, cle masquee)
  - Chargement sans cle (validation)     -> OK (message clair + code sortie 1)
  - Chargement avec la VRAIE cle .env    -> OK (cle 70 caracteres, masquee)

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 2 - api_client.py (couche transport NVIDIA NIM)
----------------------------------------------------------------
OBJECTIF :
  Module dedie aux appels HTTP vers l'endpoint NVIDIA NIM.

FICHIERS CREES :
  - retroai_agent/api_client.py   (ApiClient + ApiError)

DECISIONS TECHNIQUES :
  - Responsabilite unique : transport pur. Aucune logique d'outils
    ni de conversation ici (=> agent_loop.py).
  - requests.Session reutilisee (connexion TCP gardee, + rapide sur vieux PC).
  - Retry/backoff sur 429 via liste BACKOFF_DELAIS = [5, 10, 20]
    (3 reessais => 4 tentatives max).
  - Timeout requete = 120s.
  - Mode "thinking" injecte via chat_template_kwargs SI config.enable_thinking.
  - Cle "tools" ajoutee SEULEMENT si des outils existent (eviter tools:null/[]).
  - Exception maison ApiError pour les echecs non recuperables.

POINTS DE VIGILANCE (rappels cahier des charges) :
  - Le piege rôle "tool" -> "user" sera gere dans agent_loop.py, PAS ici.

TESTS REALISES :
  - Construction ApiClient + payload (sans reseau)  -> OK
  - VRAI appel API NVIDIA NIM ("dis bonjour")       -> OK (reponse recue,
    176 tokens, modele kimi-k2.6 confirme fonctionnel).

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 3 - safety.py (garde-fous)
----------------------------------------------------------------
OBJECTIF :
  Centraliser les confirmations interactives et la detection des
  commandes shell dangereuses. Securise par defaut.

FICHIERS CREES :
  - retroai_agent/safety.py

DECISIONS TECHNIQUES :
  - Le module AFFICHE et DEMANDE seulement, il n'execute jamais rien.
  - demander_confirmation() : defaut = NON. True uniquement si y/o/yes/oui.
    Entree seule = refus. Securise par defaut (cahier des charges).
  - try/except EOFError/KeyboardInterrupt -> refus (jamais "fail open").
  - detecter_danger() : regex sur motifs (rm -rf, mkfs, dd, fork bomb,
    /dev/sdX, shutdown/reboot...) -> avertissement renforce, n'interdit pas.

TESTS REALISES :
  - detecter_danger sur 5 commandes  -> OK (dangereuses detectees, sures = None)
  - confirmation sans terminal (EOF) -> OK (retourne False = refus)

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 4 - tools.py (les 4 outils)
----------------------------------------------------------------
OBJECTIF :
  Definir les schemas JSON des 4 outils + leur implementation Python.

FICHIERS CREES :
  - retroai_agent/tools.py

DECISIONS TECHNIQUES :
  - TOOLS_SCHEMA : format OpenAI/JSON Schema envoye a l'API.
  - Signature commune (args: dict, config: Config) -> str.
  - Chaque outil retourne TOUJOURS une str (meme en erreur) : aucune
    exception ne doit remonter et casser la boucle de l'agent.
  - read_file : troncature a 50 000 caracteres, gestion binaire
    (UnicodeDecodeError) et fichier introuvable.
  - write_file / run_shell_command : confirmation obligatoire via safety.
  - run_shell_command : detecter_danger + subprocess timeout = config.shell_timeout.
  - Table de routage TOOLS + executer_outil() pour aiguiller depuis agent_loop.

OBSTACLES / BUGS :
  - [PLATEFORME] 1er test write_file vers /tmp/ a echoue : /tmp n'existe
    pas sous Windows. Ce n'est PAS un bug code -> l'erreur a ete geree
    proprement (message clair, pas de crash). Confirme que la gestion
    d'erreur marche. A RETENIR : la cible finale est Linux Mint (/tmp ok),
    mais pour tester sous Windows utiliser %TEMP%.

TESTS REALISES :
  - list_directory, read_file (ok / absent / inconnu)  -> OK
  - write_file accepte (y) chemin valide               -> OK (fichier cree + relu)
  - write_file refuse (n)                              -> OK (fichier NON cree)
  - run_shell_command 'rm -rf' accepte (y)             -> OK (avertissement
    danger affiche, sortie capturee, code retour 0)

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 5 - agent_loop.py (orchestration + piege NIM)
----------------------------------------------------------------
OBJECTIF :
  Relier API + outils + historique. Gerer le tool calling et le piege
  du role "tool" de NVIDIA NIM.

FICHIERS CREES :
  - retroai_agent/agent_loop.py

DECISIONS TECHNIQUES :
  - Classe AgentLoop : historique = liste de messages, reset() reinjecte
    le message systeme (sert a la commande /reset).
  - *** PIEGE NIM *** : resultat d'outil reinjecte en role "user" formate
    "[Resultat de l'outil <nom>]\\n<resultat>". JAMAIS role "tool".
  - _parser_arguments() : json.loads() sous try/except (arguments = chaine
    JSON brute, possiblement malformee -> message d'erreur exploitable).
  - Boucle bornee par MAX_ITERATIONS=10 (anti-boucle infinie).
  - Le message assistant contenant les tool_calls est conserve dans
    l'historique (point de vigilance si NIM le rejetait un jour).

TESTS REALISES :
  - VRAI appel : 'liste les fichiers + compte les .py' -> OK
    * Roles historique = system/user/assistant/... AUCUN role 'tool' (PIEGE
      DESAMORCE, confirme en reel).
    * L'agent a tente run_shell_command -> confirmation refusee (EOF en test
      pipe) -> il s'est ADAPTE avec list_directory -> bonne reponse (6 .py).

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 6 - main.py (boucle CLI)
----------------------------------------------------------------
OBJECTIF :
  Point d'entree + interface utilisateur (boucle interactive).

FICHIERS CREES :
  - retroai_agent/main.py

DECISIONS TECHNIQUES :
  - main() charge la config (gere SystemExit -> exit 1 si cle manquante).
  - Commandes : /help, /reset (agent.reset()), /exit, /quit, /<inconnu>.
  - Indicateur "L'agent reflechit..." (flush=True) avant l'appel API.
  - ApiError attrapee dans la boucle -> message clair, la session continue.
  - Ctrl+C / Ctrl+D (EOFError/KeyboardInterrupt) -> sortie propre.
  - Lancement : python -m retroai_agent.main

TESTS REALISES :
  - Session pipe : /help, /reset, question reelle, /exit -> OK
    (banniere, aide, reset, reponse 'Bonjour', sortie propre).

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 7 - README.md + .gitignore (finalisation)
----------------------------------------------------------------
OBJECTIF :
  Documenter le projet et proteger la cle API du versionnement.

FICHIERS CREES :
  - README.md     (installation, config, usage, architecture, securite)
  - .gitignore    (ignore .env, __pycache__, venv, fichiers IDE)

DECISIONS TECHNIQUES :
  - .env explicitement ignore par git (jamais commit la cle).
  - README documente le piege NIM (role tool -> user) et le tableau des
    variables d'environnement.

OBSTACLES / BUGS :
  - [REDACTION] Un ``` superflu en fin de README -> corrige (Edit).

ETAT : TERMINEE ET VALIDEE.


================================================================
 PROJET COMPLET - tous les modules ecrits, testes et valides.
   Lancement : python -m retroai_agent.main
================================================================


----------------------------------------------------------------
[2026-06-18] ETAPE 8 - Renommage BAZIZ.IA + profil utilisateur
----------------------------------------------------------------
OBJECTIF :
  1) Renommer l'agent RetroAI -> BAZIZ.IA (persona, banniere, invite).
  2) Au 1er lancement, proposer (avec consentement) de renseigner nom +
     infos de base pour personnaliser l'experience.

FICHIERS CREES :
  - retroai_agent/profile.py   (consentement, collecte, stockage JSON)

FICHIERS MODIFIES :
  - retroai_agent/agent_loop.py  (SYSTEME -> "Tu es BAZIZ.IA" ;
    __init__ accepte infos_utilisateur ; reset() les injecte au systeme)
  - retroai_agent/main.py        (banniere/invite BAZIZ.IA ; appel
    profile.initialiser_profil() au demarrage ; salut par prenom)
  - .gitignore                   (ignore user_profile.json - donnees perso)

DECISIONS TECHNIQUES :
  - Consentement explicite, defaut = NON. Choix memorise (meme le refus,
    stocke {"partage": false}) pour ne plus reposer la question.
  - Profil 100% local (user_profile.json), jamais envoye, ignore par git.
  - Toutes les questions du profil sont optionnelles (Entree = saute).
  - Les infos sont injectees dans le message systeme via profil_en_texte(),
    et conservees apres /reset (car reset() reconstruit avec infos).

OBSTACLES / BUGS :
  - [AFFICHAGE/Windows] En sortie "pipe" sous Windows, les accents du
    modele s'affichent en "?" (console cp1252 vs UTF-8). PAS un bug code :
    la cible finale est Linux Mint (UTF-8 par defaut, pas de souci).
    Fix optionnel possible pour Windows si besoin (reconfigure stdout UTF-8).

TESTS REALISES :
  - 1er lancement + ACCEPTE (nom=Karim, role) -> salut par prenom +
    reponse personnalisee ("ton prenom est Karim, developpeur Python")  OK
  - 2e lancement -> aucune question, profil recharge, salut Karim         OK
  - 1er lancement + REFUS -> {"partage": false} enregistre, pas de salut,
    plus jamais redemande                                                 OK
  - Profil de test supprime apres essais (vrai 1er lancement preserve).

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 9 - Profil : pseudo au lieu du nom
----------------------------------------------------------------
OBJECTIF :
  Demander un PSEUDO et non le vrai nom (demande utilisateur).

FICHIERS MODIFIES :
  - retroai_agent/profile.py  (question "Votre pseudo :", cle "pseudo",
    profil_en_texte() et texte de consentement adaptes)
  - retroai_agent/main.py     (salut base sur profil["pseudo"])

OBSTACLES / BUGS :
  - [DONNEES] Un user_profile.json existait deja (cree par l'utilisateur
    lors d'un essai : nom=baziz, role=ingenieur info). Comme le profil
    existait, la question n'etait plus posee -> 1er test fausse. Le fichier
    a ete supprime (donnee de test) puis re-test propre effectue. A RETENIR :
    l'ancien format utilisait la cle "nom" ; desormais c'est "pseudo".
    Les anciens profils "nom" sont ignores sans erreur (lecture .get()).
  - [AFFICHAGE] Une reponse de l'agent est revenue vide une fois (content
    vide). A surveiller ; non bloquant.

TESTS REALISES :
  - 1er lancement ACCEPTE (pseudo=NeoByte) -> profil {"pseudo":"NeoByte",...}
    + salut "Ravi de te retrouver, NeoByte !"                            OK
  - Profil de test supprime (vrai 1er lancement preserve).

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 10 - Interface graphique (rich, style Claude Code)
----------------------------------------------------------------
OBJECTIF :
  Ameliorer l'interface terminal en s'inspirant de Claude Code :
  banniere en panneau, reponses en Markdown, accent orange, lignes
  d'outils stylees, spinner de reflexion, panneaux de confirmation.

FICHIERS CREES :
  - retroai_agent/ui.py   (couche d'affichage centralisee + repli sans rich)

FICHIERS MODIFIES :
  - safety.py     -> confirmation via ui.panneau_confirmation / lire_oui_non
                     (+ parametre dangereux pour panneau rouge)
  - tools.py      -> ui.action_outil(...) au lieu de print ; passe dangereux
  - agent_loop.py -> appel API entoure de ui.reflexion() (spinner)
  - main.py       -> banniere/aide/saisie/reponse/erreurs via ui
  - requirements.txt -> rich active (>=13.0.0)

DECISIONS TECHNIQUES :
  - ui.py utilise rich SI dispo, sinon repli texte simple (RICH_DISPO).
    Le projet reste 100% fonctionnel sans rich (cible = vieux PC).
  - Palette inspiree Claude Code : accent orange #D97757, danger rouge.
  - Reponses agent rendues en Markdown (Markdown de rich).
  - Spinner place autour de l'appel reseau SEUL dans agent_loop (jamais
    autour d'un input(), sinon conflit avec le mode Live de rich).
  - Force stdout/stderr en UTF-8 a l'import de ui (corrige Windows cp1252).

OBSTACLES / BUGS :
  - [BUG Windows] 1er essai : crash UnicodeEncodeError sur '✻' (cp1252).
    CAUSE : rich ecrit des glyphes Unicode dans une console Windows cp1252.
    FIX : sys.stdout/stderr.reconfigure(encoding='utf-8') au chargement de
    ui.py. Corrige aussi l'ancien souci d'affichage des accents. Sans effet
    sous Linux (deja UTF-8). -> RESOLU et re-teste OK.

TESTS REALISES :
  - Session pipe avec rich : banniere panneau, reponse Markdown (code +
    puces), accents OK, /help en panneau                              OK
  - Panneaux de confirmation rich : normal (orange) + dangereux (rouge
    "ACTION DANGEREUSE")                                              OK
  - Repli force RICH_DISPO=False : affichage texte simple, aucun crash OK
  - rich 15.0.0 installe.

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 11 - UX : rythme vertical + resultats d'outils ⎿
----------------------------------------------------------------
OBJECTIF :
  Affiner l'UI/UX facon Claude Code : meilleure respiration entre les
  blocs, et affichage du resultat des outils sous la ligne ⏺ avec ⎿.

FICHIERS MODIFIES :
  - ui.py        : action_outil() via objet Text (robuste aux crochets) +
                   ligne vide avant ; nouveau resultat_outil() (apercu ⎿,
                   tronque a 4 lignes, markup=False pour ne rien casser) ;
                   reponse_agent() avec lignes vides avant/apres (separation
                   nette des tours de parole).
  - agent_loop.py: appel ui.resultat_outil(resultat) apres chaque outil.

DECISIONS TECHNIQUES :
  - resultat_outil utilise markup=False / highlight=False : le contenu
    (fichiers, sorties shell) ne doit pas etre interprete comme du balisage.
  - Apercu limite a 4 lignes + compteur "(+N lignes)" pour rester lisible.

OBSTACLES / BUGS :
  - [DONNEES] Un user_profile.json (refus precedent de l'utilisateur)
    existait encore -> le 'n' du test est parti comme message. NON bloquant.
  - [PROCESS] DECISION : ne plus supprimer user_profile.json pendant les
    tests (ca effacait les preferences de l'utilisateur a chaque essai).
    Tests futurs : ne pas toucher au profil reel.

TESTS REALISES :
  - Session pipe : ⏺ list_directory + ⎿ apercu tronque "(+7 lignes)" +
    reponse Markdown avec puces + espacements entre tours              OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 12 - UI agrandie (gros logo + panneaux pleine largeur)
----------------------------------------------------------------
OBJECTIF :
  "Agrandir le tout" : grand logo ASCII BAZIZ.IA, panneaux pleine
  largeur, marges interieures accrues.

FICHIERS MODIFIES :
  - ui.py : constante LOGO (ASCII genere par script pour alignement fixe) ;
    banniere() -> logo centre + expand=True + padding (2,4) + lignes vides ;
    aide() et panneau_confirmation() -> expand=True + padding (1,4).

DECISIONS TECHNIQUES :
  - Logo ASCII genere via un dict de glyphes (5 lignes) pour garantir
    l'alignement des colonnes (evite les erreurs d'alignement manuel).
  - expand=True : les panneaux occupent toute la largeur du terminal.
  - En terminal, "agrandir" = plus de largeur/marges + grand logo
    (la taille de police est geree par le terminal, pas par le programme).

OBSTACLES / BUGS :
  - [ENCODAGE] Le script de generation du logo plantait en cp1252 ->
    relance avec PYTHONUTF8=1. Rappel recurrent : Windows = cp1252 par
    defaut ; le projet force deja UTF-8 dans ui.py (sans effet sous Linux).

TESTS REALISES :
  - banniere + aide a COLUMNS=90 : grand logo centre lisible "BAZIZ.IA",
    panneaux pleine largeur, marges OK                                  OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 13 - Changement de palette (orange -> rouge)
----------------------------------------------------------------
OBJECTIF :
  Passer la couleur d'accent de l'orange au rouge (demande utilisateur).

FICHIERS MODIFIES :
  - ui.py : ACCENT #D97757 -> #E03131 (rouge). DANGER assombri
    #E5484D -> #9B1C1C pour rester distinct de l'accent desormais rouge.

DECISIONS TECHNIQUES :
  - Une seule constante a changer (ACCENT) : tout le projet l'utilise.
    Illustre l'interet d'avoir centralise la palette dans ui.py.

TESTS REALISES :
  - banniere : rendu sans erreur, structure intacte (couleur visible en
    vrai terminal).                                                     OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 14 - Zone de saisie encadree (au lieu d'une fleche)
----------------------------------------------------------------
OBJECTIF :
  Remplacer la simple fleche "›" par une zone de saisie encadree
  facon Claude Code.

FICHIERS MODIFIES :
  - ui.py : lire_saisie() dessine un cadre (haut "╭─ Vous ─╮", invite
    "│ ›", bas "╰─╯") sur la largeur du terminal. try/finally pour
    toujours refermer le cadre (meme sur Ctrl+C).

DECISIONS TECHNIQUES :
  - Largeur calculee via _console.size.width (defaut 80 si non-tty).
  - Cadre haut + bas + bordure gauche seulement : input() ne permet pas
    de fermer la boite a droite (texte de longueur variable, curseur non
    controle). Une vraie boite 4 cotes exigerait prompt_toolkit (lourd) ->
    ecarte pour rester leger (cible vieux PC).

OBSTACLES / BUGS :
  - [TEST] En mode pipe (stdin non-tty), pas d'echo ni de saut de ligne
    apres input -> la bordure basse parait collee a "│ ›". Artefact de
    test uniquement : en terminal interactif, Entree descend d'une ligne
    et le rendu est correct. NON bloquant.

TESTS REALISES :
  - Affichage du cadre haut/bas pleine largeur sans erreur.            OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 15 - Confirmation : format "(y/n)"
----------------------------------------------------------------
OBJECTIF :
  Afficher l'indication "(y/n)" apres "Confirmer ?" (demande user).

NOTE : aller-retour. D'abord retire par erreur (mauvaise comprehension),
  puis remis au format "(y/n)" demande par l'utilisateur.

FICHIERS MODIFIES :
  - ui.py : lire_oui_non() -> invite "Confirmer ? (y/n)".

DECISIONS TECHNIQUES :
  - Seul l'affichage change ; la LOGIQUE de securite reste identique
    (seul y/o/yes/oui accepte, tout le reste refuse).

TESTS REALISES :
  - Panneau de confirmation : "Confirmer ? (y/n)" + "→ Accepte."       OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE 16 - Robustesse timeout + persistance (/continue)
----------------------------------------------------------------
OBJECTIF :
  1) Reessai automatique (1x) en cas de timeout/erreur API.
  2) Nettoyer l'historique si un tour echoue (pas de doublon au retry).
  3) Persistance de la conversation + commande /continue pour reprendre
     la ou on s'est arrete (eviter de perdre le contexte/tokens).

FICHIERS MODIFIES :
  - agent_loop.py :
    * CHEMIN_SESSION = "session_history.json".
    * sauver_session() / charger_session() (JSON).
    * _appel_api() : 1 essai + 1 reessai auto sur ApiError.
    * envoyer() : point_de_reprise (snapshot) ; en cas d'ApiError ->
      del historique[point_de_reprise:] (restauration) puis l'erreur
      remonte ; en cas de succes -> sauver_session().
    * boucle de dialogue extraite dans _dialoguer().
  - main.py : commande /continue -> agent.charger_session().
  - ui.py   : /continue ajoute a l'aide.
  - .gitignore : session_history.json (contient la conversation).

DECISIONS TECHNIQUES :
  - Sauvegarde apres CHAQUE tour reussi : on peut reprendre meme apres
    fermeture du programme.
  - Reponse a la question utilisateur : un timeout NE force PAS a tout
    recommencer ; l'historique est conserve (et desormais sauvegarde).

TESTS REALISES :
  - Run1 (conversation "retiens LICORNE42") -> session_history.json (3 msg) OK
  - Run2 (/continue puis "quel mot ?") -> agent repond LICORNE42 (contexte
    restaure d'un lancement a l'autre)                                   OK
  - Simulation timeout (faux chat) : 2 appels (essai+reessai), historique
    restaure a son etat initial, ApiError remontee                      OK
  - session_history.json de test supprime apres essais.

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Profil "pseudo" (et non le vrai nom)
----------------------------------------------------------------
OBJECTIF :
  Profil : demander un PSEUDO (et non le vrai nom).

FICHIERS MODIFIES :
  - profile.py : champ "nom" -> "pseudo" (question, cle JSON, texte systeme,
    message de consentement).
  - main.py    : boucle_cli(..., pseudo=profil.get("pseudo")).

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Centrage barre de saisie : TENTE PUIS ANNULE
----------------------------------------------------------------
OBJECTIF (annule) :
  Centrer horizontalement l'encadre "Vous" (largeur reduite + marge).

OBSTACLE / BUG :
  - Le centrage (marge gauche + cadre 60%) empechait le BAS du cadre
    "╰─╯" de s'afficher correctement. Sur demande utilisateur : REVERT.

ACTION :
  - ui.py / lire_saisie() remis a l'identique (cadre pleine largeur).

ETAT : ANNULE (retour a la version pleine largeur qui fonctionne).


----------------------------------------------------------------
[2026-06-18] ETAPE - Cadre de saisie : invite en coin "╰─›"
----------------------------------------------------------------
OBJECTIF :
  Le bas du cadre "╰─╯" ne s'affichait jamais pendant la frappe (capture
  cmd reduite). Cause : impossible de dessiner une ligne SOUS le curseur
  avec input() ; la ligne basse ne s'imprime qu'apres Entree -> boite
  qui parait "ouverte".

DECISION TECHNIQUE :
  - Une vraie boite 4 cotes en permanence exigerait un TUI (prompt_toolkit)
    -> ecarte (legerete, cible vieux PC).
  - A la place : invite = coin bas-gauche "╰─›". Le cadre parait ferme et
    soigne sans ligne sous le curseur. Plus de try/finally (inutile).

  Rendu :
    ╭─ Vous ───────────────────────╮
    ╰─› texte...

FICHIERS MODIFIES :
  - ui.py / lire_saisie() (RICH_DISPO).

ETAT : TERMINEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Haut du cadre sans coin + grand "AU REVOIR"
----------------------------------------------------------------
OBJECTIF :
  1) Retirer le coin "╮" en haut a droite du cadre de saisie -> finir
     par des "──" (largeur - 8 au lieu de largeur - 9 + ╮).
  2) Sur Ctrl+C / fin d'entree : afficher un grand "AU REVOIR" en
     gros caracteres pixelises (meme esprit que le LOGO).

FICHIERS MODIFIES :
  - ui.py : haut du cadre sans ╮ ; ajout _FONT_ADIEU (police pixel 5x5),
    _texte_pixel() et au_revoir() (Text rouge centre si rich).
  - main.py : boucle_cli -> ui.au_revoir() au lieu de ui.info('Au revoir')
    dans le except (EOFError, KeyboardInterrupt).

TESTS REALISES :
  - Rendu _texte_pixel('AU REVOIR')           -> OK (lisible, 5 lignes).
  - Fin d'entree (meme branche que Ctrl+C)    -> OK (grand AU REVOIR affiche).

CORRECTIF :
  - justify='center' de rich peu fiable -> centrage MANUEL : marge =
    (largeur_ecran - largeur_bloc)//2 prependee a chaque ligne.
    Verifie : ecran 120 -> bloc 54, marges 33/33.

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Echec API : CONSERVER la progression (correction)
----------------------------------------------------------------
CONTEXTE / MALENTENDU :
  L'etape precedente (robustesse timeout) SUPPRIMAIT tout le tour en cas
  d'echec (del historique[point_de_reprise:]). L'utilisateur a precise son
  vrai besoin : si l'API plante en PLEIN milieu d'une longue tache (ex.
  etape 8/10), il ne veut PAS perdre toute la reflexion deja faite.

OBJECTIF :
  En cas d'echec API, NE RIEN supprimer : conserver tous les resultats
  d'outils / etapes deja obtenus, et pouvoir reprendre la ou ca s'est
  arrete (sans tout refaire).

FICHIERS MODIFIES :
  - agent_loop.py :
    * SUPPRESSION de la logique destructrice (plus de del historique[...]).
    * Nouveau flag self.tour_incomplet (True si dernier tour interrompu).
    * envoyer() = append message + _executer_tour().
    * reprendre() = _executer_tour() SANS nouveau message (le modele
      continue a partir des derniers resultats d'outils).
    * _executer_tour() : en cas d'ApiError -> tour_incomplet=True +
      sauver_session() + raise (progression PRESERVEE). Succes ->
      tour_incomplet=False + sauver_session().
  - main.py :
    * helper _traiter_reponse(agent, saisie/reprise) : gere envoyer OU
      reprendre, et sur ApiError affiche "Progression conservee. Tape
      /continue pour reprendre".
    * /continue : si tour_incomplet -> reprendre() ; sinon recharge le
      disque, et si la session rechargee est incomplete (dernier role=user)
      -> reprise auto.
  - ui.py : libelle d'aide /continue mis a jour.

TESTS REALISES :
  - Simulation : outil execute (progression) puis timeout a l'etape
    suivante -> tour_incomplet=True, 4 messages CONSERVES (resultat
    d'outil garde), roles [system,user,assistant,user].               OK
  - reprendre() ensuite -> PAS de re-execution de l'outil, reponse
    finale obtenue, tour_incomplet=False, 5 messages.                 OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Commandes : "/" affiche la liste + COMMANDES.txt
----------------------------------------------------------------
OBJECTIF :
  1) Taper "/" (ou "/?" ou une commande inconnue) affiche la liste des
     commandes dans le terminal.
  2) Generer un fichier COMMANDES.txt documentant toutes les commandes
     (facon 'help' de cmd).

FICHIERS MODIFIES :
  - ui.py :
    * COMMANDES = source UNIQUE (liste (cmd, description)).
    * aide() utilise COMMANDES.
    * exporter_commandes(chemin="COMMANDES.txt") : ecrit le fichier depuis
      COMMANDES (reste toujours a jour).
  - main.py :
    * "/" et "/?" -> ui.aide().
    * commande inconnue -> ui.erreur(...) + ui.aide() (montre les options).
    * main() appelle ui.exporter_commandes() au demarrage.
  - COMMANDES.txt : genere a la racine.

DECISIONS TECHNIQUES :
  - Source unique COMMANDES : aide ecran + fichier txt + handler "/" tirent
    de la meme liste -> pas de divergence (DRY).
  - Fichier regenere a chaque lancement : jamais obsolete.

TESTS REALISES :
  - "/" en session -> panneau "Commandes disponibles" affiche.        OK
  - COMMANDES.txt genere, contenu complet et correct.                 OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Suggestions de commandes + doc README
----------------------------------------------------------------
OBJECTIF :
  1) Suggestions apres "/" (ex. "/c" -> propose /continue).
  2) Documenter dans le README : commandes, profil, /continue, reprise
     apres erreur, suggestions, COMMANDES.txt.

FICHIERS MODIFIES :
  - ui.py : NOMS_COMMANDES (liste plate depuis COMMANDES) ;
    commandes_correspondantes(prefixe) ; suggestions(prefixe).
  - main.py : commande non reconnue -> si prefixe correspond a des
    commandes, afficher suggestions ; sinon erreur + aide complete.
  - README.md : tableau des commandes complete (/continue), section
    Profil utilisateur, section Reprise apres erreur/timeout, astuces
    ("/", suggestions, COMMANDES.txt), note securite "(y/n)", arbo
    avec fichiers generes.

DECISIONS TECHNIQUES :
  - Suggestions = correspondance par PREFIXE (leger, fiable, multiplateforme).
  - Auto-completion "live" par Tab non incluse : necessite readline (stdlib
    sur Linux/cible, absent par defaut sous Windows). Proposable en option.

TESTS REALISES :
  - "/c" -> "Suggestions : /continue" ; "/re" -> "/reset" ;
    "/xyz" -> inconnue + liste complete.                              OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Auto-completion LIVE des commandes (prompt_toolkit)
----------------------------------------------------------------
OBJECTIF :
  Suggestions de commandes EN DIRECT (pendant la frappe de "/"), a la
  fois sous Windows (cmd) ET Linux. -> bibliotheque prompt_toolkit.

FICHIERS MODIFIES :
  - requirements.txt : ajout prompt_toolkit>=3.0.0 (optionnel).
  - ui.py :
    * import prompt_toolkit (PTK_DISPO) avec repli si absent.
    * _CommandeCompleter (Completer) : propose NOMS_COMMANDES des que la
      saisie commence par "/".
    * _obtenir_session() : creation PARESSEUSE de PromptSession (la creer
      a l'import plante hors console reelle -> NoConsoleScreenBufferError).
    * lire_saisie() : Mode1 ptk (si TTY) avec completion live -> Mode2 rich
      -> Mode3 input(). Repli si ptk echoue (sauf EOF/KeyboardInterrupt).
    * banniere() : ajout de /continue (manquait).
  - prompt_toolkit installe (3.0.52).

OBSTACLES / BUGS :
  - [BUG] PromptSession() cree a l'import -> NoConsoleScreenBufferError
    hors vraie console (terminal de test). FIX : creation paresseuse +
    try/except (repli). Import et mode pipe OK ensuite.

DECISIONS TECHNIQUES :
  - prompt_toolkit = multiplateforme (Windows + Linux), contrairement a
    readline (Linux seul). Repond a la demande "Linux ET cmd Windows".
  - Completion live active uniquement en vrai TTY (sys.stdin.isatty()) ;
    en pipe/non-tty -> repli sans completion (pas de blocage des tests).

TESTS REALISES :
  - Import + NOMS_COMMANDES + repli pipe (pas de crash)              OK
  - Banniere/aide affichent /continue                               OK
  - NOTE : la completion LIVE n'est pas testable hors vrai terminal.
  - CONFIRME PAR L'UTILISATEUR : taper "/" affiche bien le menu des
    commandes en direct dans son terminal.                            OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Fix layout completion en petite fenetre
----------------------------------------------------------------
CONTEXTE :
  L'utilisateur constate : en PLEIN ECRAN la completion s'affiche bien,
  mais en FENETRE REDUITE l'affichage est decale / grand vide (capture).

CAUSE :
  prompt_toolkit reserve par defaut 8 lignes sous le curseur pour le menu
  de completion (reserve_space_for_menu=8). En petite fenetre ce vide est
  tres visible. De plus, le cadre etait imprime par rich SEPAREMENT du
  prompt ptk -> mauvaise coordination de l'affichage.

CORRECTIF (ui.py / lire_saisie) :
  - Cadre "╭─ Vous ─" integre DANS le message du prompt ptk (HTML multi-
    ligne) au lieu d'un print rich separe -> ptk gere tout le bloc.
  - reserve_space_for_menu=4 (au lieu de 8) : moins de vide.
  - complete_style=CompleteStyle.MULTI_COLUMN : menu compact (grille).
  - Modes 2/3 (rich/input) inchanges : impriment le cadre puis lisent.

TESTS REALISES :
  - Import OK (CompleteStyle dispo) + repli pipe sans crash.          OK
  - NOTE : rendu en petite fenetre a CONFIRMER par l'utilisateur (non
    testable hors vrai terminal).

ETAT : ANNULEE (revert demande par l'utilisateur).

ANNULATION :
  L'utilisateur n'a vu aucune difference -> demande de revenir a l'etat
  d'avant. Reverts effectues :
  - import CompleteStyle retire.
  - lire_saisie() : retour a la version precedente (cadre imprime par rich
    AVANT le prompt ptk ; prompt ptk = HTML "╰─› " seul ; pas de
    reserve_space_for_menu ni de complete_style).
  Verifie : import OK, CompleteStyle absent, repli pipe OK.
  -> Le vrai probleme en petite fenetre reste a diagnostiquer (a voir si
     l'utilisateur le souhaite).


----------------------------------------------------------------
[2026-06-18] ETAPE - Commande "baziz.ia" (point d'entree / packaging)
----------------------------------------------------------------
OBJECTIF :
  Lancer l'agent en tapant juste "baziz.ia" au lieu de
  "python -m retroai_agent.main".

FICHIERS CREES :
  - pyproject.toml : metadata + [project.scripts] "baziz.ia" =
    "retroai_agent.main:main" ; dependances requests/rich/prompt_toolkit ;
    backend setuptools.

INSTALLATION :
  - pip install -e .  (mode editable : modifs du code prises en compte
    sans reinstaller).

DECISIONS TECHNIQUES :
  - Point d'entree console_scripts -> cree un executable "baziz.ia" dans
    le dossier Scripts de Python (sur le PATH).
  - Le "." dans "baziz.ia" est accepte (Windows resout via PATHEXT ->
    baziz.ia.exe ; Linux -> fichier baziz.ia).

A SAVOIR (limite) :
  - L'app lit .env / user_profile.json / session_history.json /
    COMMANDES.txt dans le DOSSIER COURANT. Donc lancer "baziz.ia" depuis
    le dossier du projet (ou definir NVIDIA_API_KEY en variable systeme).
  - Apres install, ouvrir un nouveau terminal si la commande n'est pas
    reconnue (PATH a recharger).

FICHIERS MODIFIES :
  - README.md : section "Raccourci : la commande baziz.ia" + arbo
    (pyproject.toml).

TESTS REALISES :
  - pip install -e . -> exit 0.                                        OK
  - which baziz.ia -> .../Scripts/baziz.ia ; lancement "baziz.ia" ->
    banniere + invite OK (teste en pipe).                             OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - /exit affiche le grand AU REVOIR
----------------------------------------------------------------
OBJECTIF :
  Le grand "AU REVOIR" ne s'affichait que sur Ctrl+C / Ctrl+D ; la
  commande /exit (et /quit) montrait juste "Au revoir." en texte simple.

FICHIERS MODIFIES :
  - main.py : branche /exit, /quit -> ui.au_revoir() (au lieu de
    ui.info("Au revoir.")).

TESTS REALISES :
  - "/exit" -> grand AU REVOIR pixelise affiche.                      OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Mise en ligne sur GitHub
----------------------------------------------------------------
OBJECTIF :
  Publier le projet sur GitHub.

ACTIONS (faites par l'utilisateur) :
  - Depot cree sur github.com : https://github.com/b4z1z/retroai-agent
  - git branch -M main ; git remote add origin <url> ; git push -u origin main
  - Push OK : branche main creee, suit origin/main.

SECURITE :
  - Verifie via .gitignore que .env / user_profile.json /
    session_history.json ne sont PAS suivis (seul .env.example l'est).
  - A confirmer visuellement sur la page GitHub que .env n'apparait pas.

ETAT : EN LIGNE.


----------------------------------------------------------------
[2026-06-18] ETAPE - Scripts d'installation + README Linux/Windows
----------------------------------------------------------------
OBJECTIF :
  1) Separer les instructions d'install Linux et Windows dans le README.
  2) Permettre une install en une ligne (curl | sh) facon installeurs.

FICHIERS CREES :
  - scripts/install.sh  (Linux/macOS : verifie git+python3, clone/maj,
    pip install -e ., cree .env depuis .env.example).
  - scripts/install.ps1 (Windows PowerShell : equivalent).

FICHIERS MODIFIES :
  - README.md : section Installation refondue ->
    * "Installation rapide (une ligne)" : curl|sh (Linux) et irm|iex (Win).
    * "Installation manuelle" en deux blocs <details> Linux / Windows.

REPONSE A LA QUESTION (curl one-liner) :
  - OUI possible. URL propre = raw.githubusercontent.com/<user>/<repo>/
    <branch>/<path> (le blob/...?raw=1 marche aussi via redirection -L).
  - "| sh" = Linux/macOS seulement ; Windows -> script .ps1 via irm|iex.

A FAIRE PAR L'UTILISATEUR :
  - git add . ; git commit ; git push  (les one-liners ne marcheront
    qu'une fois scripts/ pousse sur la branche main de GitHub).

ETAT : TERMINEE (a pousser).


----------------------------------------------------------------
[2026-06-18] ETAPE - Signature / branding "made by baziz"
----------------------------------------------------------------
OBJECTIF :
  Apposer la signature de l'auteur (baziz) a plusieurs endroits pour
  l'attribution / la reconnaissance du projet.

FICHIERS MODIFIES :
  - __init__.py   : docstring "Made by baziz" + __author__ + __url__.
  - ui.py         : banniere -> ligne "made by baziz · github.com/b4z1z"
    (versions rich ET fallback texte).
  - pyproject.toml: authors = [{ name = "baziz", email = ... }].
  - README.md     : section "Auteur" (Made by baziz + lien GitHub).

NOTE :
  - La signature = branding (effacable). La VRAIE preuve d'anteriorite =
    l'historique git (commits dates + nom/email) + un fichier LICENSE.
  - LICENSE non encore choisie (decision en attente de l'utilisateur).

TESTS REALISES :
  - Banniere affiche "made by baziz · github.com/b4z1z".              OK

ETAT : TERMINEE (LICENSE a faire plus tard).


----------------------------------------------------------------
IDEES FUTURES (a faire plus tard, pas aujourd'hui)
----------------------------------------------------------------
- [ ] SYSTEME DE PLUGINS : permettre d'ajouter de nouveaux outils a
      l'agent sans modifier le coeur. Piste : un dossier "plugins/",
      chaque plugin expose un schema JSON + une fonction ; chargement
      dynamique au demarrage et fusion dans TOOLS / TOOLS_SCHEMA.
- [ ] TUTO TERMINAL interactif (onboarding) : un vrai tutoriel guide
      DANS le terminal au 1er lancement (ou via commande /tuto), plus
      helpful que les messages actuels : explique les commandes, fait
      essayer un exemple, montre la confirmation y/n, etc.
- [x] BOUTON STOP (interrompre sans quitter) : FAIT le 2026-06-19.
      Ctrl+C pendant la reflexion -> stop + retour au prompt sans quitter,
      progression conservee (tour_incomplet) -> /continue reprend.
      CONFIRME OK par l'utilisateur en vrai terminal.
- [x] AJOUT DE PHOTOS / IMAGES : FAIT le 2026-06-19. Le modele kimi-k2.6
      via NIM SUPPORTE la vision (teste : repond la bonne couleur). Module
      images.py : detecte les chemins d'images dans le message, encode en
      base64 data-URI, construit le content multimodal (text + image_url).
      Voir l'etape detaillee plus bas.
- [ ] GESTION HISTORIQUE facon CLAUDE CODE : plusieurs sessions par projet,
      avec liste/choix (comme "claude --resume") et reprise de la derniere
      (comme "claude --continue"). Aujourd'hui on a juste 1 session_history
      .json + /continue ; etendre vers plusieurs conversations nommees,
      horodatees, listables et selectionnables.
- [ ] LICENSE (choix en attente : MIT ou Tous droits reserves).
- [ ] Interrupteur de test des pannes API (simuler un echec a l'etape N).
- [~] Tests automatises (pytest) : FAIT pour securite/config/images
      (2026-06-19, 15 tests). Reste plus tard : ui, agent_loop, tools.


----------------------------------------------------------------
[2026-06-18] ETAPE - README : clarifier .env vs .env.example
----------------------------------------------------------------
OBJECTIF :
  Preciser dans le README qu'on met la cle UNIQUEMENT dans .env, jamais
  dans .env.example (qui est public sur GitHub).

FICHIERS MODIFIES :
  - README.md : section "Obtenir une cle API" -> encadre + tableau
    .env vs .env.example (lequel est suivi par git, ou mettre la cle).

ETAT : TERMINEE.


----------------------------------------------------------------
[2026-06-18] ETAPE - install.sh robuste (Linux : pip/venv + raccourci)
----------------------------------------------------------------
CONTEXTE :
  Sur Linux Mint l'utilisateur a eu "No module named pip" et le raccourci
  baziz.ia disparaissait a chaque nouveau terminal (venv non active).
  Question : est-ce que curl|sh telecharge bien tous les outils utilises ?

CLARIFICATION :
  - Les dependances (requests, rich, prompt_toolkit) sont declarees dans
    pyproject.toml [project.dependencies]. "pip install -e ." les installe
    AUTOMATIQUEMENT. Pas besoin de les lister dans install.sh.

FICHIERS MODIFIES :
  - scripts/install.sh refondu pour Linux :
    1. installe git/python3/python3-pip/python3-venv via apt si manquants ;
    2. clone/maj le depot ;
    3. cree un venv (.venv) -> evite PEP 668 -> pip install -e . (=> tire
       requests/rich/prompt_toolkit depuis pyproject.toml) ;
    4. cree un lien ~/.local/bin/baziz.ia (commande globale, sans activer
       le venv car le script du venv pointe vers son propre python) et
       ajoute ~/.local/bin au PATH (.bashrc) si absent ;
    5. cree .env depuis .env.example.

NOTE :
  - install.ps1 (Windows) laisse tel quel : pip present par defaut, pas de
    PEP 668, baziz.ia va dans Scripts/ (deja sur le PATH).

ETAT : TERMINEE (a pousser pour que le curl|sh utilise la nouvelle version).


----------------------------------------------------------------
[2026-06-18] ETAPE - install.sh : methode pipx (choix utilisateur)
----------------------------------------------------------------
OBJECTIF :
  Remplacer la methode venv+symlink par pipx (choix de l'utilisateur)
  pour une commande "baziz.ia" globale, isolee, sans venv a activer.

FICHIERS MODIFIES :
  - scripts/install.sh : installe git/python3/pipx (apt si besoin),
    pipx ensurepath, clone/maj, puis "pipx install --force ." (qui tire
    requests/rich/prompt_toolkit depuis pyproject.toml), cree .env.
  - README.md : section "Raccourci" -> Linux/macOS = pipx (recommande),
    Windows = pip dans un venv. Note mise a jour : git pull + pipx
    install --force .

POURQUOI pipx :
  - Installe une APPLICATION CLI de facon isolee, commande sur le PATH
    (~/.local/bin via pipx ensurepath), dispo dans tous les terminaux.
  - Evite l'erreur PEP 668 et le besoin d'activer un venv a chaque fois.

RAPPEL : baziz.ia lit .env dans le dossier courant -> lancer depuis le
  dossier du projet.

ETAT : TERMINEE (a pousser).


----------------------------------------------------------------
[2026-06-19] ETAPE - README : section Prerequis (outils a avoir)
----------------------------------------------------------------
OBJECTIF :
  Documenter les outils requis (curl, git, python3) avec "comment
  verifier / comment installer si ca ne marche pas", par OS.

FICHIERS MODIFIES :
  - README.md : nouvelle section "Prerequis (outils a avoir)" avec
    tableaux Linux/macOS et Windows (verifier via --version, installer
    via apt / liens officiels), + notes de depannage (command not found,
    PATH a recharger, option "Add to PATH" sous Windows).

ETAT : TERMINEE (a pousser).


----------------------------------------------------------------
[2026-06-19] ETAPE - README : section "Reinitialiser ses donnees"
----------------------------------------------------------------
OBJECTIF :
  Expliquer comment remettre a zero le pseudo / les infos / l'historique.

FICHIERS MODIFIES :
  - README.md : nouvelle sous-section "Reinitialiser ses donnees /
    parametres" -> tableau (user_profile.json = pseudo/infos ;
    session_history.json = conversation ; les deux = reset complet) +
    commandes rm (Linux) / del (Windows) + rappel commande /reset.

ETAT : TERMINEE (a pousser).


----------------------------------------------------------------
[2026-06-19] DEBUG - Erreur API sur Linux (403 puis 500) -> RESOLU
----------------------------------------------------------------
SYMPTOME :
  Sur Linux, baziz.ia renvoyait "HTTP 403 forbidden / authorization failed".

DIAGNOSTIC (methode) :
  - Test direct via curl (sans passer par le code) pour isoler.
  - 403 = rejet a l'AUTHENTIFICATION -> la cle elle-meme est en cause.
  - CAUSE : le .env Linux avait ete cree par l'installeur (cp .env.example
    .env) -> contenait le PLACEHOLDER (ou une cle mal copiee), pas la vraie.

CORRECTION :
  - Mettre la VRAIE cle (celle qui marche sous Windows) dans le .env Linux.
  - Apres correction : passage de 403 -> 500.
    * 500 = erreur SERVEUR NVIDIA (l'auth est passee) = transient.
    * Resolu en reessayant (le serveur a refonctionne).

LECONS :
  - 403/401 = probleme de CLE ; 500 = probleme SERVEUR (souvent passager).
  - Tester la cle au curl direct est le meilleur moyen d'isoler cle vs code.
  - L'installeur cree .env depuis .env.example -> TOUJOURS y mettre sa vraie
    cle ensuite (sinon 403 garanti).

ETAT : RESOLU - BAZIZ.IA fonctionne sur Linux.


----------------------------------------------------------------
[2026-06-19] ETAPE - Bouton STOP (Ctrl+C interrompt sans quitter)
----------------------------------------------------------------
OBJECTIF :
  Pouvoir stopper l'agent en pleine reflexion / boucle d'outils avec
  Ctrl+C, revenir au prompt SANS quitter l'appli, et CONSERVER la
  progression (reprise possible via /continue).

FICHIERS MODIFIES :
  - agent_loop.py : _executer_tour() attrape desormais aussi
    KeyboardInterrupt (en plus d'ApiError) -> tour_incomplet=True +
    sauver_session() + raise. La progression (resultats d'outils, etapes)
    est preservee.
  - main.py : _traiter_reponse() -> sur KeyboardInterrupt appelle
    ui.stop_reflexion() (au lieu d'un simple "[Interrompu.]").
  - ui.py : nouvelle fonction stop_reflexion() (message "⏹ Reflexion
    stoppee. Tape /continue pour reprendre", rich + fallback texte).

COMPORTEMENT :
  - Ctrl+C PENDANT la reflexion/les outils -> stop + retour au prompt
    (appli reste ouverte) + progression conservee -> /continue reprend.
  - Ctrl+C / Ctrl+D a l'invite (idle) -> quitte l'appli (grand AU REVOIR),
    comportement inchange (standard CLI).
  - Ctrl+C pendant une confirmation y/n -> refus (securite, inchange).

TESTS REALISES :
  - Simulation outil execute puis KeyboardInterrupt -> remonte au main,
    tour_incomplet=True, 4 messages conserves (progression OK).        OK
  - Message stop_reflexion() affiche correctement.                     OK
  - App demarre sans erreur (smoke test).                              OK
  - NOTE : le Ctrl+C reel pendant le spinner rich est a confirmer en
    vrai terminal (non testable en pipe).

ETAT : TERMINEE (a confirmer Ctrl+C reel cote terminal).


----------------------------------------------------------------
[2026-06-19] ETAPE - Interface en ANGLAIS + indicateur "Ctrl+C to stop"
----------------------------------------------------------------
OBJECTIF :
  1) Traduire TOUS les messages d'interface en anglais (produit client).
  2) Ajouter un indicateur montrant comment stopper -> place dans le
     spinner de reflexion : "Thinking… (Ctrl+C to stop)".

FICHIERS MODIFIES (messages utilisateur -> anglais) :
  - ui.py : banniere, saluer ("Welcome back"), cadre saisie ("You"),
    reponse vide, stop_reflexion ("Stopped / Type /continue to resume"),
    reflexion ("Thinking… (Ctrl+C to stop)"), [TOOL], "(+N lines)",
    COMMANDES (descriptions EN), suggestions, aide ("Available commands"),
    exporter_commandes, panneau_confirmation ("DANGEROUS ACTION /
    Confirmation required / Action:"), lire_oui_non ("Confirm?").
    + au_revoir : "AU REVOIR" -> "GOODBYE" (police pixel : ajout G,D,B,Y,
    retrait V,U,R,I,A devenus inutiles -> en fait garde A; glyphes
    G/O/D/B/Y/E presents).
  - main.py : /reset "History cleared", /continue (Resuming / Previous
    session restored / Incomplete task detected / No previous session),
    "Unknown command", message ApiError ("Progress saved...").
  - safety.py : MOTIFS_DANGEREUX (EN), "Confirm?", "Approved/Refused".
  - tools.py : tous les messages d'outils (erreurs, apercu File/Size/
    Preview, Command, WARNING, exit code, bytes...) en anglais.
  - agent_loop.py : SYSTEME en anglais (reponds dans la langue de
    l'utilisateur) ; "[Tool result: ...]", limite, retry, erreurs.
  - api_client.py : messages ApiError (timeout, network, 429, HTTP, JSON).
  - config.py : message NVIDIA_API_KEY manquante en anglais.
  - profile.py : consentement + questions (nickname...) en anglais.

NOTE :
  - SYSTEME demande desormais a l'agent de repondre DANS LA LANGUE DE
    L'UTILISATEUR (donc FR si l'utilisateur ecrit en FR). Seule l'interface
    (chrome) est en anglais.
  - README laisse en francais (doc), non concerne par la demande.

TESTS REALISES :
  - Banniere/aide/GOODBYE en anglais, rendu OK.                       OK
  - Confirmation "DANGEROUS ACTION / Confirm? (y/n) / Refused".        OK
  - Spinner affiche "Thinking… (Ctrl+C to stop)".                     OK

ETAT : TERMINEE.


----------------------------------------------------------------
[2026-06-19] ETAPE - Support des IMAGES (vision / multimodal)
----------------------------------------------------------------
OBJECTIF :
  Envoyer une image a l'agent en referencant son chemin dans le message
  (ex. "describe photo.png").

VERIFICATION PREALABLE (cruciale) :
  - Test direct API : PNG rouge genere sans dependance (struct/zlib),
    envoye en content multimodal {text + image_url base64}. Le modele a
    repondu "Red" -> kimi-k2.6 via NIM SUPPORTE la vision. OK.

FICHIERS CREES :
  - images.py : extraire_chemins_images, encoder_image (data-URI base64),
    construire_contenu(texte) -> (str, []) si pas d'image, sinon
    (liste multimodale [text + image_url], [noms d'images]). MAX 8 Mo.

FICHIERS MODIFIES :
  - agent_loop.py : envoyer() utilise images.construire_contenu() ; si
    images -> ui.image_jointe(nom) + content = liste multimodale.
    SYSTEME : "if the user references an image, it is ALREADY attached:
    look at it directly, don't inspect it with shell tools".
  - ui.py : image_jointe(nom) -> "🖼 image attached: <nom>".

OBSTACLES / BUGS :
  - [BUG] 1er test : ponctuation collee au chemin ("photo.png?") -> ext
    ".png?" non reconnue -> image NON jointe ; le modele a DEVINE "Red"
    depuis le nom "test_rouge" (faux positif trompeur !). FIX : _nettoyer()
    retire ponctuation de fin + parentheses/guillemets/@.
  - RE-TEST nom NEUTRE : image bleue -> "Blue" ; verte -> "green square".
    Preuve qu'il VOIT vraiment l'image. OK.

LIMITE CONNUE :
  - Images base64 stockees dans session_history.json -> fichier volumineux
    + re-envoi a chaque tour. Acceptable pour l'instant.

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-19] ETAPE - Commandes /add-image et /paste (images sans chemin)
----------------------------------------------------------------
OBJECTIF :
  Joindre une image SANS taper le chemin :
  - /add-image : ouvre une fenetre de selection de fichier (gestionnaire)
  - /paste     : recupere l'image du presse-papiers (ex. capture d'ecran)
  (Rappel : coller une image directement dans le terminal texte est
   impossible -> ces commandes contournent la limite.)

FICHIERS MODIFIES :
  - images.py : choisir_image_dialogue() (tkinter filedialog) ;
    image_depuis_presse_papiers() (Pillow ImageGrab -> PNG temporaire) ;
    construire_contenu(texte, chemins_explicites) accepte des images en plus.
  - agent_loop.py : envoyer(message, chemins_images=None).
  - ui.py : COMMANDES += /add-image, /paste (visibles dans menu "/" et aide) ;
    demander_texte() pour la legende de l'image.
  - main.py : handlers /add-image et /paste -> _envoyer_avec_image() (demande
    une legende puis _traiter_reponse avec chemins_images) ; _traiter_reponse
    accepte chemins_images.
  - pyproject.toml / requirements.txt : ajout pillow>=9.0.0.
  - scripts/install.sh : ajout python3-tk aux prerequis apt (pour /add-image).
  - README.md : 3 facons d'envoyer une image (chemin, /add-image, /paste).

DECISIONS :
  - tkinter (stdlib) pour le selecteur -> pas de dep pip (Linux: python3-tk).
  - Pillow pour le presse-papiers (multiplateforme). Reuse l'attachement
    multimodal deja en place (envoyer affiche "image attached").
  - Repli propre : si presse-papiers vide / Pillow absent / tkinter absent
    -> message d'erreur clair, l'appli continue.

TESTS REALISES :
  - imports OK ; NOMS_COMMANDES contient /add-image et /paste.        OK
  - /help affiche les 2 nouvelles commandes.                          OK
  - /paste sans image -> "No image found in the clipboard...".        OK
  - Pillow 12.2.0 present sous Windows -> /paste pret a l'usage.       OK
  - App demarre sans erreur.                                          OK
  - NOTE : test reel du dialogue/presse-papiers a faire en vrai terminal
    (GUI non disponible en pipe).

ETAT : TERMINEE (test GUI a confirmer cote utilisateur).


----------------------------------------------------------------
[2026-06-19] ETAPE - Liste blanche commandes sures (auto-exec opt-in)
----------------------------------------------------------------
OBJECTIF :
  Eviter de demander confirmation pour les commandes shell LECTURE SEULE
  (ls, cat, echo, find, grep...) -> plus rapide. MAIS securise et opt-in.

FICHIERS MODIFIES :
  - config.py : Config.auto_safe_commands (bool) ; AUTO_SAFE_COMMANDS,
    defaut False.
  - .env.example : AUTO_SAFE_COMMANDS=false (+ explication).
  - safety.py : COMMANDES_SURES (liste blanche lecture seule),
    METACARACTERES_DANGEREUX (> | < & ; $ ` (){} \n), FIND_FLAGS_DANGEREUX
    (-exec/-execdir/-delete/-fprint/-fls/-ok), est_commande_sure().
  - tools.py : run_shell_command -> si config.auto_safe_commands ET
    est_commande_sure() -> auto-exec sans confirmation (avec message
    "(safe read-only command...)"). Sinon confirmation comme avant.

SECURITE (le point cle) :
  - Juger "safe" sur le 1er mot ne suffit PAS : "echo x > f" ecrit, "find
    -delete" supprime, "echo $(rm)" substitue, "a ; rm" chaine.
  - est_commande_sure() exige : 1er mot dans liste blanche + AUCUN
    metacaractere + pas de flag find dangereux. Au moindre doute -> False.
  - OPT-IN, defaut OFF -> reste "ultra-securise par defaut" (cahier des
    charges). Confirmation 100% tant que l'option n'est pas activee.

OBSTACLES / BUGS :
  - [BUG] "find" oublie dans COMMANDES_SURES (alors que son cas special
    etait code) -> "find . -name x" refuse a tort. FIX : ajout de find
    (+ cut, uniq, nl, tac, column).

TESTS REALISES :
  - est_commande_sure : 16 cas (surs True, pieges echo>/find -delete/
    -exec/;/$()/|/&&/backtick/sed -i False) -> TOUS CORRECTS.
  - Flux reel option ON : 'echo' auto-exec sans confirmation ; 'echo x > f'
    redemande confirmation (garde-fou).                                OK

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-19] ETAPE - Tests automatises (pytest) : securite/config/images
----------------------------------------------------------------
OBJECTIF :
  Poser un filet de securite (pytest) sur les parties STABLES + CRITIQUES,
  conformement a l'approche hybride decidee (tester ce qui ne bouge plus
  et ce qui est risque ; laisser le reste pour plus tard).

FICHIERS CREES :
  - tests/test_safety.py : detecter_danger (dangereuses/sures) + est_commande
    _sure (liste blanche OK ; pieges > | ; && $() backtick / find -delete/
    -exec / hors liste / vide -> bloques).
  - tests/test_config.py : cle manquante -> SystemExit ; valeurs par defaut ;
    conversion bool/int ; timeout invalide -> defaut. (monkeypatch + dotenv
    inexistant pour ne pas lire le vrai .env.)
  - tests/test_images.py : detection chemin ; ponctuation collee (bug du dev) ;
    fichier inexistant ignore ; str sans image vs liste multimodale avec
    image (vrai PNG cree via tmp_path).

FICHIERS MODIFIES :
  - pyproject.toml : [project.optional-dependencies] dev = ["pytest>=7.0"].
  - .gitignore : .pytest_cache/.
  - README.md : section "Tests" (pip install -e ".[dev]" ; pytest).

LANCEMENT :
  - pip install -e ".[dev]"  puis  pytest

RESULTAT :
  - 15 tests PASSED en 0.59s (Windows, pytest 9.1.1, Python 3.14).

REMARQUE :
  - Approche hybride : UI/agent_loop (encore mouvants) testes plus tard,
    quand stabilises. La securite est verrouillee des maintenant.

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-21] ETAPE - Connaissance permanente du createur (BAZIZ / B4Z1Z)
----------------------------------------------------------------
OBJECTIF :
  Que l'agent sache repondre a "qui est BAZIZ / B4Z1Z ?" (identite du
  createur), distinct du profil optionnel de l'utilisateur courant.

FICHIER MODIFIE :
  - agent_loop.py : nouvelle constante A_PROPOS (bio createur : BAZIZ /
    handle B4Z1Z / bazizdev07@gmail.com / repo github). Injectee dans
    reset() : systeme = SYSTEME + A_PROPOS [+ infos_utilisateur].
    Editable librement pour completer la bio.

ETAT : TERMINEE.


----------------------------------------------------------------
[2026-06-21] ETAPE - Polish images (3 ameliorations)
----------------------------------------------------------------
OBJECTIF :
  Alleger et fiabiliser le support image.

FICHIERS MODIFIES :
  - images.py :
    (1) URLs distantes http(s) : detectees dans le texte ET passees telles
        quelles a l'API (encoder_image renvoie l'URL sans encoder).
    (2) Reduction des grandes images via Pillow : si max dimension > 1568px,
        thumbnail() puis re-encodage (JPEG q85, ou PNG si canal alpha).
        Repli silencieux sur le fichier brut si Pillow absent/echec.
        Filet MAX_OCTETS conserve avant ET apres reduction.
    (3) alleger_pour_disque(historique) : remplace les data-URI base64 par
        une note texte courte AVANT sauvegarde (URLs distantes conservees).
        L'historique en memoire reste intact (image visible pendant la session).
  - agent_loop.py : sauver_session() ecrit images.alleger_pour_disque(...)
    -> session_history.json ne gonfle plus de plusieurs Mo.

VERIF :
  - URL pass-through + detection OK.
  - alleger : base64 -> "[image omitted...]", URL distante gardee.
  - reduction : 2000x2000 PNG -> 1568x1568 JPEG.
  - 15 tests pytest toujours OK.

ETAT : TERMINEE ET VALIDEE.


----------------------------------------------------------------
[2026-06-21] ETAPE - Generation d'images : /create-image (FLUX via NIM)
----------------------------------------------------------------
OBJECTIF :
  Creer des images depuis une description texte. IMPORTANT : kimi-k2.6 ne
  GENERE pas d'images -> on utilise un modele SEPARE (FLUX.1-dev) sur
  l'endpoint genai de NVIDIA, avec la MEME cle API NVIDIA_API_KEY.

CHOIX : FLUX.1-dev par defaut (meilleure qualite), SWITCHABLE via .env
  (IMAGE_MODEL=..., ex. stabilityai/sdxl-turbo pour du plus rapide).

FICHIERS MODIFIES / CREES :
  - config.py : champs image_base_url (defaut ai.api.nvidia.com/v1/genai) et
    image_model (defaut black-forest-labs/flux.1-dev), lus depuis IMAGE_BASE_URL
    / IMAGE_MODEL. Defauts dans la dataclass (n'impacte pas les tests).
  - api_client.py : methode generer_image(prompt, width/height/steps/cfg/seed)
    -> POST {image_base}/{model}, payload mode=base, retry/backoff 429 comme
    chat(). Helper _extraire_image_base64() TOLERANT (artifacts[].base64 /
    data[].b64_json / champ direct).
  - image_gen.py (NOUVEAU) : creer_image() decode le base64 et enregistre un
    PNG dans generated_images/ (nom horodate), retourne le chemin.
  - main.py : commande /create-image -> demande la description -> spinner
    "Generating image…" -> ui.image_creee(chemin). Ctrl+C = annule.
  - ui.py : /create-image ajoute a COMMANDES (menu deroulant + COMMANDES.txt
    auto) ; helper image_creee().
  - .gitignore : generated_images/.
  - .env.example : IMAGE_MODEL + IMAGE_BASE_URL documentes.

VERIF :
  - imports OK ; extraction base64 (3 formats + cas vide) OK ; defaut config
    OK ; /create-image dans le menu OK ; 15 tests pytest OK.

A TESTER EN REEL :
  - L'appel a l'endpoint genai (format requete/reponse reconstitue depuis la
    doc ; extraction tolerante au cas ou). -> lancer /create-image une fois.

ETAT : CODE TERMINE, A VALIDER EN REEL.

  AJOUT : apres generation, l'image s'ouvre automatiquement dans la
  visionneuse par defaut (image_gen.ouvrir_image : os.startfile / open /
  xdg-open selon l'OS ; echec silencieux -> message si non ouvrable).


----------------------------------------------------------------
[2026-06-21] ETAPE - Multi-fournisseurs images + menu /image (FLUX / Nano Banana)
----------------------------------------------------------------
OBJECTIF :
  Permettre de generer avec Nano Banana (Gemini) en plus de FLUX, sans
  laisser de cle dans le repo open source : chaque utilisateur saisit SA
  cle in-app (ecrite dans .env gitignore). FLUX reste le defaut (out of the box).

DESIGN UX (decide avec l'utilisateur) :
  - On GARDE /create-image, /add-image, /paste tels quels.
  - /image = panneau de REGLAGE du modele de generation (n'affiche/genere rien) :
    montre le modele courant + les commandes + 3 options (1 FLUX, 2 Nano Banana
    Pro, 3 Nano Banana Flash). Enter = garder.
  - Choisir Gemini sans cle -> l'app demande la cle UNE fois et l'ecrit dans
    .env (set_env_value). Sessions suivantes : memorise. Pas d'edition manuelle.
  - Pas d'OAuth (trop lourd + pas gratuit + client_id dans le repo) : la cle
    API EST la "connexion" au compte Google (generee sur aistudio.google.com).

FICHIERS MODIFIES / CREES :
  - config.py : champs image_provider/gemini_api_key/gemini_model + lecture env
    (IMAGE_PROVIDER/GEMINI_API_KEY/GEMINI_MODEL). NOUVEAU set_env_value(nom,
    valeur) : cree/MAJ une ligne dans .env + os.environ (preserve commentaires).
  - image_gen.py : label_modele() ; _generer_gemini() (endpoint generateContent,
    header x-goog-api-key, responseModalities TEXT+IMAGE) ; _extraire_gemini_
    base64() (parts[].inlineData.data) ; creer_image() aiguille selon provider.
  - main.py : commande /image -> _menu_image() (affiche + change le modele,
    demande/sauve la cle Gemini si besoin). Pro=gemini-3-pro-image,
    Flash=gemini-2.5-flash-image.
  - ui.py : /image dans COMMANDES ; menu_image(courant, gemini_pret).
  - .env.example : IMAGE_PROVIDER + GEMINI_API_KEY (commente) + GEMINI_MODEL.

VERIF :
  - extraction Gemini OK (+ cas vide) ; labels OK ; sans cle -> ApiError clair ;
    set_env_value cree ET met a jour sans dupliquer ; /image dans le menu ;
    15 tests pytest OK.

A TESTER EN REEL :
  - /create-image avec FLUX (NVIDIA) ; puis /image -> 2 (saisir cle Gemini) ->
    /create-image avec Nano Banana. (format requete/reponse Gemini reconstitue
    depuis la doc ; extraction tolerante.)

ETAT : CODE TERMINE, A VALIDER EN REEL.

  AJOUT : gestion fin de palier gratuit Gemini. _generer_gemini() leve
  QuotaError (sous-classe ApiError) sur HTTP 429. main._gerer_quota affiche
  ui.quota_atteint() (panneau : switch FLUX / attendre reset / upgrade key)
  puis propose "Switch to FLUX (free) and retry now? (y/n)" -> bascule
  IMAGE_PROVIDER=nvidia (persiste .env) et regenere aussitot. Refus -> astuce
  (quota reset quotidien, /image pour changer).


----------------------------------------------------------------
[2026-06-21] ETAPE - Aide logiciel par l'IA elle-meme
----------------------------------------------------------------
OBJECTIF :
  Que l'IA serve aussi de MANUEL : si l'utilisateur demande "how to add an
  image ?", "quelles commandes ?", "comment generer une image ?", elle aide
  au lieu de repondre "je suis un assistant textuel".

FICHIERS MODIFIES :
  - agent_loop.py : nouvelle constante AIDE_LOGICIEL (liste des commandes
    slash + comment envoyer une image pour analyse vs en creer + limite du
    coller-image terminal). Injectee dans reset() :
    systeme = SYSTEME + A_PROPOS + AIDE_LOGICIEL [+ infos_utilisateur].
  - ui.py : aide() (/help) et exporter_commandes() (COMMANDES.txt) signalent
    "Need help using BAZIZ.IA? Just ask me".

VERIF : commandes presentes dans AIDE_LOGICIEL ; aide() OK ; 15 tests OK.
ETAT : TERMINEE.

  CORRECTIF : /create-image accepte desormais la description EN LIGNE
  (/create-image un chat) en plus de la forme interactive. L'IA suggerait
  cette forme alors qu'elle n'existait pas -> code aligne sur l'aide.

  AIDE_LOGICIEL reecrite pour coller EXACTEMENT au comportement reel de
  chaque commande : regle "seul /create-image accepte du texte en ligne",
  detail du panneau /image (3 modeles + cle Gemini), /add-image (dialogue,
  python3-tk), /paste (Pillow), attache via chemin OU URL. Evite que l'IA
  invente une syntaxe inexistante.


----------------------------------------------------------------
[2026-06-21] FIX - "empty response" sur longue generation (ex. code TASM)
----------------------------------------------------------------
CAUSE :
  max_tokens=4096 (en dur) + mode thinking : sur une longue sortie, le
  raisonnement epuisait le budget -> coupure avant le contenu -> "" affiche
  comme "empty response".
FIX :
  - config.py : champ max_tokens (defaut 8192), lu depuis MAX_TOKENS.
  - api_client.py : payload utilise config.max_tokens (au lieu de 4096).
  - agent_loop.py : si contenu vide ET finish_reason == "length", message
    clair (augmente MAX_TOKENS / ENABLE_THINKING=false) au lieu d'un vide.
  - .env.example : MAX_TOKENS=8192 documente.
VERIF : defaut 8192 ; 15 tests OK.
ETAT : TERMINEE.

  AJUST : max_tokens defaut passe a 0 = AUCUNE limite (non envoye dans le
  payload -> le modele genere jusqu'a son max). Plafond optionnel via
  MAX_TOKENS>0. L'utilisateur ne veut pas de limite par message (l'API
  plafonne par debit, pas par requete).


----------------------------------------------------------------
[2026-06-21] ETAPE - Modes d'approbation (facon Claude Code, Shift+Tab)
----------------------------------------------------------------
OBJECTIF :
  Cycler entre des modes d'approbation, defaut sur (normal), affiches
  seulement quand pertinent (barre du bas si != normal).

MODES (modes.py, etat global, cycle Shift+Tab) :
  - normal     : tout confirme (defaut).
  - auto-edit  : ecritures fichiers auto-approuvees ; shell confirme.
  - plan       : lecture seule -> write_file/run_shell_command BLOQUES,
    + message systeme injecte (envoyer) "ne rien modifier, proposer un plan".
  - auto-all   : tout auto-approuve (yolo).

FICHIERS CREES/MODIFIES :
  - modes.py (NOUVEAU) : NORMAL/AUTO_EDIT/PLAN/AUTO_ALL, ORDRE, courant(),
    cycler(), definir(), label(), auto_edits()/auto_tout()/est_plan().
  - tools.py : write_file -> bloque si plan, auto si auto_edits ; 
    run_shell_command -> bloque si plan, auto si auto_tout (ou safe opt-in).
  - agent_loop.py : envoyer() prefixe une consigne plan ; import modes.
  - ui.py : Shift+Tab (s-tab) cycle via KeyBindings ; bottom_toolbar
    _barre_mode (vide si normal -> masquee) ; helper mode_actuel() ;
    /mode dans COMMANDES.
  - main.py : commande /mode (cycle) et /mode <nom> (fixe).
  - AIDE_LOGICIEL : description des 4 modes pour que l'IA aide.

SECURITE : defaut reste 'normal' (sur). Modes auto = choix explicite user.
VERIF : cycle OK ; plan bloque l'ecriture (fichier non cree) ; 15 tests OK.
A TESTER EN REEL : Shift+Tab dans un vrai terminal (binding prompt_toolkit).
ETAT : CODE TERMINE, A VALIDER EN REEL.

  UI : barre du bas (bottom_toolbar) supprimee (jugee moche). A la place,
  Shift+Tab affiche le nouveau mode en TEXTE SIMPLE sans surlignage, via
  run_in_terminal(mode_actuel), au moment du changement. /mode idem.

  Modes : ajoutes a /help (bloc "Approval modes - Shift+Tab to cycle",
  rich + texte). Astuce ui.astuce_modes() affichee a chaque demande de
  confirmation (safety.demander_confirmation) -> l'user sait qu'il peut
  passer en auto-accept pour eviter les confirmations.

  Astuce modes CONTEXTUELLE : demander_confirmation(categorie="edit"/"command")
  -> ui.astuce_modes precise quel auto-accept saute CETTE confirmation
  (edits = ecritures seulement ; commandes = auto-accept all).


----------------------------------------------------------------
[2026-06-21] FIX - Ctrl+C : confirmation + reactivite pendant le thinking
----------------------------------------------------------------
PROBLEME :
  Ctrl+C sur une confirmation ne faisait que refuser CETTE action (le modele
  relancait -> boucle). Et Ctrl+C pendant l'appel API etait lent/inactif
  (requests bloquant, signal traite a la fin ; pire sous Windows).
FIX :
  - safety.demander_confirmation : ne capture plus KeyboardInterrupt (seul
    EOFError = refus). Ctrl+C remonte -> _executer_tour le capte -> stop du
    tour (comme le bouton stop) + progression conservee.
  - agent_loop : _executer_interruptible(fn) lance l'appel dans un thread
    daemon et join(0.1) en boucle -> Ctrl+C reactif (~100ms). _appel_api
    l'utilise. Le thread abandonne finit en arriere-plan (resultat ignore).
VERIF : helper OK (resultat + exceptions) ; 15 tests OK.
ETAT : TERMINEE (a confirmer en reel).

  Robustesse : erreur NIM 500 "ResourceExhausted / Worker request limit"
  (saturation transitoire) detectee via _est_saturation() -> reessai backoff
  comme un 429 (chat + generer_image) ; si persiste, message court clair
  MSG_SATURATION au lieu du dump JSON. Vraie erreur serveur = message detaille.

  CORRECTIF : la limite NIM (ResourceExhausted) n'est PAS reessayee (reessayer
  ne debloque pas une limite atteinte). Message clair immediat MSG_SATURATION
  couvrant les 2 cas (capacite worker saturee OU quota d'utilisation epuise).


----------------------------------------------------------------
[2026-06-21] ETAPE - /add-file : joindre un fichier texte/code
----------------------------------------------------------------
FICHIERS :
  - files.py (NOUVEAU) : choisir_fichier_dialogue() (tkinter, tout fichier),
    lire_fichier_texte() (-> (contenu, erreur) ; binaire/absent geres ;
    tronque a 100k chars), construire_message_fichier() (nom + bloc delimite).
  - main.py : commande /add-file (selecteur OU /add-file <chemin>), handler
    _ajouter_fichier (lit, demande message optionnel, envoie a l'agent).
  - ui.py : /add-file dans COMMANDES ; helper fichier_joint() ; import os.
  - agent_loop.py : /add-file decrit dans AIDE_LOGICIEL (vs /add-image images).
VERIF : lecture/binaire/absent/menu OK ; 15 tests OK.
ETAT : TERMINEE (a tester en reel via le selecteur).


----------------------------------------------------------------
[2026-06-21] ETAPE - /compose : editeur temporaire (long message / code)
----------------------------------------------------------------
FICHIERS :
  - files.py : _editeur_par_defaut ($VISUAL/$EDITOR sinon notepad/nano) +
    composer_dans_editeur() (fichier temp via mkstemp, lance l'editeur en
    bloquant avec shlex.split pour gerer "code -w", relit le contenu, supprime
    le temp). Retourne texte / "" (vide) / None (editeur introuvable).
  - main.py : commande /compose -> _composer() (ouvre l'editeur, envoie le
    texte non vide a l'agent).
  - ui.py : /compose dans COMMANDES.
  - agent_loop.py : /compose dans AIDE_LOGICIEL (suggerer pour coller bcp de
    code -> regle aussi le scroll de la ligne de saisie).
BUT : coller du code / long texte sans encombrer l'invite (probleme scroll).
VERIF : compose renvoie le texte (faux editeur) ; defaut notepad ; 15 tests OK.
ETAT : TERMINEE (a tester en reel avec nano/notepad).

  AJOUTS /compose :
   - /compose <texte> : pre-remplit l'editeur (composer_dans_editeur(initial)).
   - Message tres long (>=500 chars OU >=6 lignes) tape a l'invite -> ouverture
     auto de l'editeur pre-rempli (_est_long + _editer_si_long dans main).
     Pas d'editeur dispo -> envoi tel quel. Vide -> annule.
   - VS Code : EDITOR="code -w".

  AJOUTS :
   - /compose : apres l'editeur, demande une NOTE optionnelle (placee avant le
     bloc) -> _ajouter_note().
   - /write : saisie MULTI-LIGNE inline (ui.lire_multiligne) terminee par une
     ligne '.' ou Ctrl-D ; alternative a l'editeur externe (rien a fermer).
     Ctrl-C annule. Ajoute au menu + AIDE_LOGICIEL.

  DECISION : Shift+Enter impossible (prompt_toolkit refuse 's-enter' + terminaux
  n'en distinguent pas le code) -> multi-ligne = Alt+Entree (+ /write). Auto-
  ouverture editeur retiree. /compose mis EN COURS DE DEV (stub ui.en_developpement,
  "(coming soon)" dans /help et AIDE_LOGICIEL) ; code editeur garde dans files.py
  pour plus tard.

  /add-file : verifie de bout en bout (chemin fourni -> contenu + consigne
  assembles et envoyes). Marche aussi en direct "/add-file <chemin>" si le
  selecteur tkinter n'est pas dispo (python3-tk).
  /write : passe EN COURS DE DEV (stub en_developpement, "(coming soon)") ;
  multi-ligne via Alt+Entree. Lecteur ui.lire_multiligne garde pour plus tard.

  FIX REGRESSION : "/add-file ne fait rien" -> cause = multiline=True + override
  de la touche Entree (experimentation multi-ligne) qui empechait l'envoi des
  commandes. Revert a la saisie fiable (Entree=envoi, seul Shift+Tab garde).
  Multi-ligne Alt+Entree mis de cote (a reprendre). + selecteur de fichier
  force au premier plan (focus_force/update, parent=racine) pour /add-file et
  /add-image, + repli "demander le chemin" si pas de dialogue, + nettoyage des
  guillemets autour du chemin.


----------------------------------------------------------------
[2026-06-21] ETAPE - Niveaux de reflexion /think (low..ultra)
----------------------------------------------------------------
  thinking.py (NOUVEAU) : NIVEAUX low/medium/high/highx/ultra ; est_actif()
  (thinking ON/OFF) + consigne() (effort injecte au message). kimi n'ayant
  qu'un thinking ON/OFF, on simule les niveaux.
  config.py : champ thinking_level (THINKING_LEVEL env, sinon derive de
  ENABLE_THINKING). api_client : payload thinking selon le niveau. agent_loop :
  injecte la consigne d'effort par message. main : /think <niveau>|cycle +
  persiste dans .env. ui : niveau_thinking() + /think au menu. AIDE_LOGICIEL.
  ULTRA regle pour la QUALITE DU CODE (complet/runnable, edge cases, idiomes,
  compile mentalement). max_tokens illimite -> code non coupe.
  VERIF : niveaux OK, payload OK, 15 tests OK.

  FIX #1 (mot coupe en mode fenetre) : rich rendait des lignes plus larges que
  la fenetre visible (largeur buffer) -> terminal coupait au milieu des mots.
  ui._largeur_visible() (os.get_terminal_size = fenetre visible) + 
  ui._rafraichir_console() (recree Console(width=visible)) appele a chaque tour
  dans lire_saisie -> suit le resize, retour a la ligne sur mots entiers. Cadre
  saisie / au_revoir alignes (marge 1 colonne).

  DECISION #1 (suite) : le word-wrap de la LIGNE DE SAISIE n'est pas supporte
  par prompt_toolkit (coupe au caractere ; cf issue #1068). Claude Code y arrive
  car il est en Node/Ink (composant maison). On GARDE le char-wrap (cosmetique,
  texte envoye correct). -> IDEE FUTURE : composant de saisie word-wrap maison.
  (Le word-wrap de la SORTIE rich, lui, est corrige.)

  FIX timeout : requete API timeout 120->300s, configurable (REQUEST_TIMEOUT,
  config.request_timeout). TimeoutApiError(ApiError) ; _appel_api NE reessaie
  PLUS un timeout (re-attente inutile). Message d'aide (baisser /think, monter
  REQUEST_TIMEOUT, decouper). IDEE FUTURE : streaming (sortie au fil de l'eau,
  pas de timeout, feedback live).
  + /add-file CONFIRME fonctionnel en reel (pingpong.asm 6822 chars joint).


----------------------------------------------------------------
[2026-06-21] ETAPE - Streaming des reponses (anti-timeout, façon Claude)
----------------------------------------------------------------
  config.stream (STREAM env, defaut true). api_client : chat() dispatche
  _chat_simple (ancien) / _chat_stream (SSE). _lire_flux() accumule texte +
  tool_calls (fragments) et appelle on_texte(fragment) -> meme structure de
  reponse (agent_loop inchange). ui.creer_stream_printer() (printer+cloturer)
  affiche le texte BRUT en direct (en-tete a la 1ere fois). agent_loop._appel_api :
  branche streaming (pas de spinner/retry ; Ctrl+C reactif car flux frequent) ;
  flag _stream_affiche. main : ne reaffiche pas si deja streame.
  /think selecteur a fleches (ui.selecteur via radiolist_dialog) + DESCRIPTIONS.
  Timeout requete 300s configurable (REQUEST_TIMEOUT) ; TimeoutApiError pas
  reessaye. BUT du streaming : repond au besoin "partie par partie, appels pas
  longs" (la reponse arrive en continu, plus de timeout).
  VERIF : parseur SSE (texte+tool_calls) OK ; 15 tests OK.

  Streaming UX : ajout d'un spinner _Attente (thread) "Thinking… (Ctrl+C)"
  affiche AVANT le 1er fragment (sinon ca paraissait fige, surtout /think ultra).
  S'efface au 1er morceau de reponse. creer_stream_printer gere spinner +
  affichage. Repli STREAM=false si le SSE de NIM ne marche pas.

  Comportement "ecrit dans le fichier" (facon Claude) : SYSTEME demande
  d'utiliser write_file (sauver le resultat) au lieu d'imprimer tout le code
  dans le chat quand on demande de creer/modifier/ameliorer un fichier.
  files.construire_message_fichier inclut le CHEMIN ABSOLU + consigne "save with
  write_file to this exact path". tools.write_file : apercu elargi (1500 chars
  + nb lignes) pour VOIR le code. Astuce : Shift+Tab auto-accept edits = ecrit
  sans confirmer.

----------------------------------------------------------------
[2026-07-05] PASSE UX STREAMING (fable 5) - polish complet
----------------------------------------------------------------
  1. THINKING VISIBLE EN DIRECT : _lire_flux extrait delta.reasoning_content
     (ou .reasoning) et l'envoie au printer avec reflexion=True. Le printer
     affiche : spinner -> en-tete "✻ Thinking…" + raisonnement EN GRIS (ANSI 90)
     au fil de l'eau -> en-tete "⏺ BAZIZ.IA" + reponse. Le raisonnement n'est
     PAS conserve dans l'historique (pas de bloat contexte).
  2. FIX messages synthetiques avales : "[Limit reached...]" et "[cut off...]"
     forcaient _stream_affiche=False -> main les affiche meme en streaming.
  3. Header SSE : Accept: text/event-stream sur la requete stream.
  4. (tour precedent) MAX_ITERATIONS 10->25 configurable + consigne "write the
     file ONCE then STOP" contre les boucles d'outils.
  VERIF : mock SSE raisonnement+reponse+tool_calls OK ; raisonnement absent du
  content ; cloturer() True seulement si reponse affichee ; 15 tests OK.
  A VALIDER EN REEL : rendu du thinking gris + ecriture fichier en 1 passe.

----------------------------------------------------------------
[2026-07-05] ETAPE - Finition /compose + /write + tests + validation reelle
----------------------------------------------------------------
  /compose TERMINE : fichier temp DEDIE (fermer sa fenetre/onglet ne touche
  pas aux autres fichiers ; VS Code EDITOR="code -w" n'attend que cet onglet).
  Ligne d'instructions MARQUEUR_COMPOSE en tete, retiree a l'envoi. Lecture
  utf-8-sig (BOM notepad) + normalisation CRLF. /compose <texte> pre-remplit.
  /write TERMINE : multi-ligne inline, fin par ligne '.' seule ou Ctrl-D,
  Ctrl-C annule. Menu /help et AIDE_LOGICIEL mis a jour (plus de coming soon).
  TESTS : +4 fichiers (test_api_client : flux SSE texte/raisonnement/outils,
  saturation, extraction image ; test_modes ; test_thinking ; test_files :
  lecture + compose BOM/CRLF/marqueur via faux editeur). 15 -> 40 PASSED.
  VALIDATION REELLE (API NVIDIA) : chat streaming OK — 4 fragments de
  raisonnement + reponse assemblee correcte + finish stop. Le thinking gris
  s'affichera bien en live.
  ETAT : TERMINE ET VALIDE.

----------------------------------------------------------------
[2026-07-05] AJUST - Raisonnement masque + methode de travail par etapes
----------------------------------------------------------------
  DEMANDE : ne pas montrer le raisonnement brut ; a la place, sur un long
  projet, travailler proprement etape par etape (plan puis execution).
  - ui.creer_stream_printer : les fragments de raisonnement ne sont PLUS
    affiches (ils servent de signe de vie) ; seul le spinner tourne, avec le
    TEMPS ECOULE ("Thinking… 12s"). La reponse reste streamee en direct.
  - agent_loop.SYSTEME : nouvelle section METHOD - pour une tache longue :
    1) plan numerote court, 2) execution etape par etape ("Step 2/4 - ..."),
    UN fichier / une partie coherente par etape, jamais tout d'un coup.
    Progression sauvegardee a chaque etape (reprise /continue).
    Regle FILES reformulee par fichier ("write each file ONCE").
  VERIF : raisonnement invisible, spinner avec secondes, reponse OK ; 40 tests.
